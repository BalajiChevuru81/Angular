import { FastrackDirective } from './fastrack.directive';

describe('FastrackDirective', () => {
  it('should create an instance', () => {
    const directive = new FastrackDirective();
    expect(directive).toBeTruthy();
  });
});
